# Direct dependencies are exact-pinned. requirements.lock resolves and
# hash-pins the complete transitive graph used by the container build.

fastapi==0.139.0
uvicorn[standard]==0.51.0
httpx==0.28.1
apscheduler==3.11.3
hvac==2.4.0
requests==2.34.2
pydantic-settings==2.14.2
psycopg[binary]==3.3.4

# private_key_jwt client assertions for the Anthropic WIF Keycloak
# exchange (app/drivers/anthropic_wif.py). Exact-pinned: signing-path
# dependency added for a security fix. [crypto] pulls in cryptography
# for RS/ES signing.
PyJWT[crypto]==2.13.0
cryptography==49.0.0

opentelemetry-api==1.43.0
opentelemetry-sdk==1.43.0
opentelemetry-exporter-otlp-proto-http==1.43.0
opentelemetry-instrumentation-fastapi==0.64b0
